# Cli-Proxy 环境变量配置
# 复制此文件为 .env 并填入实际值
# 注意: .env 文件不应提交到版本控制

# ============ JWT 配置 ============
# JWT 签名密钥 - 必须修改！
# 生成方式: openssl rand -hex 32
JWT_SECRET=your-jwt-secret-change-me-in-production

# ============ 数据库配置 ============
# MySQL 连接信息
DB_HOST=mysql
DB_USER=cli-proxy
DB_PASSWORD=your-db-password-change-me
DB_NAME=cli-proxy

# MySQL Root 密码 (仅用于 docker-compose 初始化)
MYSQL_ROOT_PASSWORD=your-root-password-change-me

# ============ 管理员配置 ============
# 默认管理员密码 (可选)
# 如果不设置，首次启动时会生成随机密码并打印到日志
# ADMIN_INITIAL_PASSWORD=your-admin-password
